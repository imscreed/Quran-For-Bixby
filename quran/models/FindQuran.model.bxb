action (FindQuran) {
  type (Search)
  description (Finds quran audio item using a searchTerm)
  collect {
    input (searchTerm) {
      type (SearchTerm)
      min (Optional) max (One)
    }
  }
  output (QuranAudio)
}
